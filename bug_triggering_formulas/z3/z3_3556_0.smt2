(declare-fun a () (Set Int))
(declare-fun b () (Set Int))
(assert (distinct a b))
(push)
(check-sat)
(declare-fun a () (Set Int))
(declare-fun b () (Set Int))
(assert (distinct a b))
(push)
(check-sat)
