(declare-fun a (Int) Int)
(declare-fun b (Int) Int)
(assert (distinct a b))
(check-sat)
(assert (distinct a b))
(check-sat)
(declare-fun a (Int) Int)
(declare-fun b (Int) Int)
(assert (distinct a b))
(check-sat)
(assert (distinct a b))
(check-sat)
